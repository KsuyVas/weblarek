# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные 

В приложении используются две сущности, которые описывают данные, — товар и покупатель. 

#### Интерфейс Товар (IProduct)
Описывает структуру данных товара, получаемого с сервера:
`id: string` - уникальный идентификатор товара
`description: string` - описание товара
`image: string` - ссылка на изображение товара
`title: string` - название товара
`category: string` - категория товара
`price: number | null` - цена товара (null означает бесценный товар)

#### Интерфейс Покупатель (IBuyer)
Содержит данные покупателя для оформления заказа:
- `payment: TPayment` - способ оплаты ('card' или 'cash')
- `email: string` - электронная почта
- `phone: string` - номер телефона
- `address: string` - адрес доставки

### Модели данных
Для учёта данных в приложении используются три класса, которые будут разделены между собой по смыслу и зонам ответственности

#### Класс Catalog 
Хранит товары, которые можно купить в приложении.

Конструктор: 

Поля класса:
`_items: IProduct[]`- хранит массив всех товаров;
`_selectedItem: IProduct | null`- хранит товар, выбранный для подробного отображения

Методы класса:
`setItems(items: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода;
`getItems():IProduct[]` - получение массива товаров из модели;
`getItem(id: string): IProduct | undefined` - получение одного товара по его id;
`setSelectedItem(item: IProduct): void` - сохранение товара для подробного отображения;
`getSelectedItem(): IProduct | null` - получение товара для подробного отображения.

#### Класс Basket 
Хранит товары, которые пользователь выбрал для покупки.

Конструктор:

Поля класса:
`_items: IProduct[]` - хранит массив товаров, выбранных покупателем для покупки.

Методы класса:
`getItems(): IProduct[]` - получение массива товаров, которые находятся в корзине;
`addItem(item: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины;
`removeItem(itemId: string): void` - удаление товара, полученного в параметре из массива корзины;
`clear(): void` - очистка корзины;
`getTotalPrice(): number` - получение стоимости всех товаров в корзине;
`getCount(): number` - получение количества товаров в корзине;
`contains(itemId: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода.

#### класс  Buyer
Содержит данные покупателя, которые тот должен указать при оформлении заказа.

Конструктор

Поля класса: 
`_payment: TPayment | null` - хранит вид оплаты.
`_address: string` - хранит адреc.
`_phone: string` - хранит телефон.
`_email: string` - хранит email.
`_errors: IFormErrors` - хранит объект с ошибками валидации.

Методы класса:
`setPayment(payment: TPayment): void` - сохранение вида оплаты
`setAddress(address: string): void` - сохранение адреса 
`setPhone(phone: string): void` - сохранение телефона    
`setEmail(email: string): void` - сохранение email
`getBuyerData():  IBuyer` - получение всех данных покупателя.
`clear(): void` - очистка данных покупателя.
`validate(): IFormErrors` - валидация данных. Обратите внимание, что правила валидации описаны в функциональных требованиях. Поле является валидным, если оно не пустое. Метод валидации должен давать возможность определить не только валидность каждого отдельного поля, но и предоставлять информацию об ошибке, связанной с проверкой конкретного значения.

интерфейс IFormErrors {
    payment?: string;
  address?: string;
  email?: string;
  phone?: string;
}


### Слой коммуникации

#### Класс LarekAPI
Отвечает за взаимодействие с сервером интернет-магазина. Получает данные о товарах и отправляет данные о заказах.

Конструктор:
`constructor(api: IApi)` - Принимает объект, соответствующий интерфейсу IApi базовый класс Api из стартового набора.

Поля класса:
`_api: IApi` - хранит экземпляр класса Api для выполнения HTTP-запросов.

Методы класса:
`getProductList(): Promise<IProduct[]>` - Выполняет GET запрос на эндпоинт /product/ и возвращает массив товаров.
`orderProduct(order: IOrder): Promise<IOrderResult>` - Выполняет POST запрос на эндпоинт /order/ и передаёт данные заказа, возвращает подтверждение покупки. 


### Слой представления

Все компоненты представления наследуются от базового класса Component<T>. Компоненты разделены на три иерархии:

Card — иерархия карточек товара (родитель содержит только общие для всех карточек поля)

Form — иерархия форм (родитель содержит общую логику валидации и управления формой)

Самостоятельные компоненты — Modal, Header, Gallery, Basket, Success

Каждый компонент генерирует события при взаимодействии пользователя. События обрабатываются в Презентере.

#### Класс  Card<T>
Абстрактный базовый класс для всех вариантов карточек товара. Cодержит только те поля и методы, которые есть ВО ВСЕХ дочерних карточках.

Конструктор:

`constructor(container: HTMLElement, actions?: ICardActions)` - принимает DOM-элемент, который будет служить корневым контейнером карточки и объект с колбэками для обработки действий пользователя

Поля класса:
`_title: HTMLElement` -	элемент для заголовка.
`_price:	HTMLElement` -	элемент для цены.

Методы класса:
`set title(value: string)` -	устанавливает заголовок
`set price(value: number | null)` -	устанавливает цену (если null, отображает "Бесценно")

#### Класс Form<T>
Абстрактный базовый класс для всех форм оформления заказа. Объединяет общую логику валидации, отображения ошибок и управления состоянием кнопки.


Конструктор:
`constructor(container: HTMLFormElement, events?: IEvents)` - принимает ...

Поля класса:
`_form: HTMLFormElement` -	DOM-элемент формы.
`_errors: HTMLElement` - 	элемент для отображения ошибок.
`_submitButton: HTMLButtonElement` -	кнопка отправки формы.
`_inputs: HTMLInputElement[]` -	массив всех полей ввода.
`` -	элемент для цены.

Методы класса:
`set valid(value: boolean)` -	активирует/деактивирует кнопку отправки.
`set errors(value: string)` -	устанавливает текст ошибки.
`protected onInputChange(field: keyof T, value: string)` -	обработчик изменения полей (генерирует событие).
`clear()` -	очищает все поля формы.
`render(data?: Partial<T>): HTMLElement` -	переопределяет родительский метод.

Конкретные компоненты

#### Класс Modal
Управляет отображением модального окна и его содержимым. Это единое модальное окно, в которое подставляется разный контент. Не имеет дочерних классов.

Конструктор:
`constructor(container: HTMLElement, events?: IEvents)` - принимает ....

Поля класса:
`modalContainer: HTMLElement` -	селектор .modal
`_content: HTMLElement` -	селектор .modal__content.
`_closeButton: HTMLElement` -	селектор .modal__close.

Методы класса:
`open(): void` -	открывает модальное окно (добавляет класс modal_active, блокирует скролл).
`close(): void` -	закрывает модальное окно (удаляет класс modal_active, разблокирует скролл).
`set content(value: HTMLElement)` -	устанавливает содержимое модального окна.
`render(data?: object): HTMLElement` -	переопределяет родительский метод.

(Генерируемые события: modal:open	при открытии окна, modal:close	при закрытии окна)

#### Класс  Header
Управляет шапкой сайта и счётчиком корзины.

Конструктор:
`` - ....

Поля класса:
`_basketButton: HTMLButtonElement` -	селектор .header__basket.
`_counterElement:HTMLElement` - селектор .header__basket-counter.

Методы класса:
`set counter(value: number)` -	устанавливает значение счётчика.

(Генерируемые события: basket:open	при клике на кнопку корзины)


#### Класс  Gallery
Управляет отображением галереи товаров на главной странице.

Конструктор:

`constructor(container: HTMLElement)` - принимает DOM-элемент.

Поля класса:
`_container: HTMLElement` -	селектор .gallery.

Методы класса:
`set items(items: HTMLElement[])` -отображает массив карточек в галерее (заменяет содержимое).
`addItem(item: HTMLElement)` -	добавляет одну карточку в конец галереи. СКОРЕЕ ВСЕГО УБРАТЬ
`clear(): void` -	очищает галерею. // - СКОРЕЕ ВСЕГО УБРАТЬ

#### Класс  CardCatalog
Наследует класс  Card. Карточка товара для отображения в каталоге.

Поля класса:
`_category: HTMLElement` -	селектор .card__category.
`_image: HTMLImageElement` -	селектор .card__image.

Методы класса:
`set category(value: string)` -	устанавливает категорию и соответствующий CSS-класс для цвета.
`set image(value: string)` -	устанавливает изображение.
	
Генерируемые события:
card:select	{ id: string } -	при клике на карточку


#### Класс  CardPreview
Наследует класс  Card.Детальная карточка товара для отображения в модальном окне. Показывает полную информацию о товаре, позволяет добавить/удалить из корзины.

Поля класса:
`_category: HTMLElement` -	селектор .card__category.
`_image: HTMLImageElement` -	селектор .card__image.
`_description: HTMLElement` -	селектор .card__text.
`_button: HTMLButtonElement` -	селектор .card__button.

Методы класса:
`set category(value: string)` -	устанавливает категорию и соответствующий CSS-класс для цвета.
`set image(value: string)` -	устанавливает изображение.
`set description(value: string)` -	устанавливает описание.
`set buttonText(value: string)` -	устанавливает текст кнопки.
`set disabled(value: boolean)` -	блокирует/разблокирует кнопку.
	
Генерируемые события:
card:addToBasket	{ id: string } -	при клике на кнопку "В корзину"
card:removeFromBasket	{ id: string } -	при клике на кнопку "Удалить из корзины"

Особенности:

Кнопка меняет текст в зависимости от того, в корзине товар или нет

Если цена null, кнопка становится неактивной с текстом "Недоступно"

#### Класс CardBasket
Наследует класс  Card. Карточка товара для отображения в корзине. Показывает товар в списке корзины с возможностью удаления.

Поля класса:
`_index:	HTMLElement` -	селектор .basket__item-index.
`_deleteButton: HTMLButtonElement` -	селектор .basket__item-delete.

Методы класса:
`set index(value: number)` -	устанавливает порядковый номер товара.
	
Генерируемые события:
basket:remove	{ id: string }	- при клике на кнопку удаления.

#### Класс Basket
Компонент корзины для отображения в модальном окне/ Отображает список выбранных товаров, общую сумму и кнопку оформления.

Поля класса:
`_list: HTMLElement` -	селектор .basket__list.
`_totalPrice: HTMLElement` -	селектор .basket__price.
`_orderButton	HTMLButtonElement` -	селектор .basket__button.

Методы класса:
`set items(items: HTMLElement[])` -	устанавливает список товаров (если пусто — показывает "Корзина пуста").
`set totalPrice(value: number)` -	устанавливает общую стоимость.
`set buttonDisabled(value: boolean)` -	блокирует/разблокирует кнопку оформления.
	
Генерируемые события:
basket:order	при клике на кнопку "Оформить".

#### Класс OrderForm 
Наследует Form<IOrderForm>. Форма первого шага оформления заказа: выбор способа оплаты и ввод адреса доставки.

Поля класса:
`_paymentButtons: NodeListOf<HTMLButtonElement>` -	кнопки выбора способа оплаты.
`_addressInput: HTMLInputElement` -	поле ввода адреса.

Методы класса:
`set payment(value: 'card' | 'cash' | null)` -	визуально выделяет выбранную кнопку оплаты.
`set address(value: string)` -	устанавливает значение поля адреса.
`clear()` -	очищает форму.
	
Генерируемые события:
order.paymentChange	{ payment: 'card' | 'cash' } - при изменении способа оплаты
order.addressChange	{ address: string } - при изменении адреса
order.submit	{ payment, address } - при отправке формы

#### Класс ContactsForm 
наследует Form<IContactsForm>. Форма второго шага оформления заказа: ввод email и телефона покупателя.

Поля класса:
`_emailInput: HTMLInputElement` -	поле ввода email.
`_phoneInput: HTMLInputElement` -	поле ввода телефона.

Методы класса:
`set email(value: string)` -	устанавливает значение email.
`set phone(value: string)` -	устанавливает значение телефона.
`clear()` -	очищает форму.
	
Генерируемые события:
contacts.emailChange { email: string } -	при изменении email
contacts.phoneChange { phone: string } -	при изменении телефона
contacts.submit { email, phone } - при отправке формы

#### Класс Success 
Компонент успешного оформления заказа. Отображает подтверждение заказа и списанную сумму.

Поля класса:
`_total: HTMLElement` -	селектор .order-success__description.
`_closeButton: HTMLElement` -	селектор .order-success__close.

Методы класса:
`set total(value: number)` -	устанавливает текст "Списано X синапсов".
	
Генерируемые события:
success:close - при клике на кнопку

### События приложения
Все события в приложении передаются через брокер событий EventEmitter. События используются для связи между слоями View и Presenter. Компоненты View генерируют события при взаимодействии пользователя, а Presenter подписывается на эти события и реагирует на них.

#### События модального окна
##### modal:open 
Сигнализирует о том, что модальное окно открыто. Presenter может использовать это для блокировки скролла или других действий
Генерирует	Modal	при открытии модального окна

##### modal:close
Сигнализирует о закрытии модального окна. Presenter может очистить содержимое или разблокировать скролл
Генерирует	Modal	gри закрытии модального окна

#### События корзины (навигация)
##### basket:open
Запрашивает открытие корзины. Presenter должен собрать данные корзины и отобразить компонент Basket в модальном окне.
Генерирует	Header при клике на иконку корзины в шапке сайта

#### События карточек товаров
##### card:select
Запрашивает показ детальной информации о товаре. Presenter должен найти товар по id в модели каталога и отобразить CardPreview в модальном окне
Генерирует	CardCatalog при клике на карточку товара в каталоге
Данные	{ id: string } — идентификатор выбранного товара

##### card:addToBasket
Запрашивает добавление товара в корзину. Presenter должен добавить товар в модель корзины и обновить счётчик в шапке
Генерирует	CardPreview при клике на кнопку "В корзину" в детальной карточке товара
Данные	{ id: string } — идентификатор добавляемого товара

##### card:removeFromBasket
Запрашивает удаление товара из корзины. Presenter должен удалить товар из модели корзины и обновить счётчик в шапке, а также изменить текст кнопки обратно на "В корзину".
Генерирует	CardPreview при клике на кнопку "Удалить из корзины" в детальной карточке товара (когда товар уже в корзине)
Данные	{ id: string } — идентификатор удаляемого товара


##### basket:remove
Запрашивает удаление товара из корзины. Presenter должен удалить товар из модели корзины и обновить отображение корзины и счётчик в шапке.
Генерирует	CardBasket при клике на кнопку удаления товара в списке корзины
Данные	{ id: string } — идентификатор удаляемого товара

##### basket:order
Запрашивает переход к оформлению заказа. Presenter должен открыть форму OrderForm в модальном окне.
Генерирует	Basket при клике на кнопку "Оформить" в корзине.

#### События формы оформления заказа (адрес и оплата)
##### order.paymentChange
Уведомляет об изменении способа оплаты. Presenter должен сохранить это значение в модели покупателя и проверить валидность формы.
Генерирует	OrderForm при выборе способа оплаты (онлайн или при получении)
Данные	{ payment: 'card' | 'cash' } — выбранный способ оплаты

##### order.addressChange
Уведомляет об изменении адреса. Presenter должен сохранить значение в модели покупателя и проверить валидность формы.
Генерирует	OrderForm  при изменении текста в поле "Адрес доставки"
Данные	{ address: string } — текущее значение адреса

##### order.submit
Сигнализирует о завершении первого шага оформления. Presenter должен открыть вторую форму ContactsForm в модальном окне.
Генерирует	OrderForm при отправке формы (клик на кнопку "Далее")
Данные	{ payment: 'card' | 'cash' | null, address: string } — все данные формы

#### События формы контактов (email и телефон)	
##### contacts.emailChange
Уведомляет об изменении email. Presenter должен сохранить значение в модели покупателя и проверить валидность формы.
Генерирует	ContactsForm при изменении текста в поле "Email"
Данные	{ email: string } — текущее значение email

##### contacts.phoneChange
Уведомляет об изменении телефона. Presenter должен сохранить значение в модели покупателя и проверить валидность формы
Генерирует	ContactsForm при изменении текста в поле "Телефон"
Данные	{ phone: string } — текущее значение телефона
	
##### contacts.submit
Сигнализирует о завершении второго шага оформления. Presenter должен собрать полные данные заказа (товары из корзины + данные покупателя) отправить их на сервер и обработать ответ.
Генерирует	ContactsForm при отправке формы (клик на кнопку "Оплатить")
Данные	{ email: string, phone: string } — все данные формы

#### Событие успешного оформления
##### success:close
Закрывает модальное окно с подтверждением. Presenter должен очистить корзину, очистить данные покупателя и обновить счётчик в шапке
Генерирует	Success при клике на кнопку "За новыми покупками!"
